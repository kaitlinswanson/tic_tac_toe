// use a module to create the gameBoard object as an array inside of it. 

const gameBoard = (function() => {
  const cells = document.querySelectorAll('board');
  let gridCells = ['', '', '', '', '', '', '', '', '']; 

  const cellSelected = 

   return {
      
        
    };
  })();

//create displayController using module

// use factories to create players